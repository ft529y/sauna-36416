<%= render "shared/header_logo" %>

<div class="procedure">
  <h2>退会お手続き</h2>
  <p>
    退会手続きをされますと、全てのサービスがご利用いただけなくなります。<br>
    再度ご利用をいただく場合には、新規登録のお手続きが必要になります。<br>
    本当に退会してよろしいですか？
  </p>
</div>

  <div class="withdrawal-wrap">
    <div class="withdrawal-btn"><%= link_to "退会する", withdrawal_path(@user), method: :patch %></div>
    <div class="no-withdrawal-btn"><%= link_to "退会しない", user_path(current_user) %></div>
  </div>

<footer class="footer-container">
  <div class="copy-rights"><%= render "shared/footer_logo"%></div>
</footer>